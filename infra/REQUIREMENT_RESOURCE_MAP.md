# Requirement-to-resource and cost map

| Requirement | Implemented behavior | Terraform resource/module | Validation | Cost implication |
|---|---|---|---|---|
| DLV-005, GCP-000 | Explicit dev-only foundation/runtime states; staging/prod are non-executable definitions. | `bootstrap`, `environments/dev`, `environments/dev_runtime` | fmt, init/validate, mock tests, state-prefix and workflow scans | Terraform itself has no service charge; provisioned resources do. |
| GCP-004, DLV-008 | Versioned uniform-access remote state and three keyless GitHub OIDC phases restricted to immutable owner/repository IDs plus exact names/main/direct workflow/environment. Plan state writes are lock-only; apply writes are runtime-prefix-only. Versioning and seven-day soft deletion recover state without a retention policy that traps `.tflock`. | State bucket, custom/conditional state roles and `github_wif` | Exact immutable-ID/name WIF tests, state-condition/retention-absence tests, disposable lock probe, phase separation and no-key/basic-role scan | Low bucket storage/operation cost; WIF has no expected material direct cost. |
| DLV-003 | One non-root combined SPA/API image deployed only by immutable foundation-repository digest; a separate non-deployed test stage contains locked integration dependencies. | Docker multi-stage build; Artifact Registry | Docker build, UID/dependency-isolation assertions, source/digest validation, cleanup-policy test | Private image storage and egress; cleanup deletes any version older than seven days after keeping the 20 most recent plus the single `rollback-current` predecessor. |
| APP-002, DLV-001 | PostgreSQL runtime with checked migrations before cloud readiness and an idempotent evidence job. | `cloud_sql`, local PostgreSQL Compose, Cloud Run startup migration and migration job | Compose ordering; Alembic/advisory-lock/startup tests; connector/static checks | Cloud SQL is expected to dominate dev cost; price and tier availability require a target-region plan. |
| SEC-001, SEC-002 | Cloud Run is IAM-private; development headers remain behind Cloud IAM and production is not configured. | `cloud_run`, foundation project-level deploy invoker and no runtime service IAM member | `invoker_iam_disabled=false`; public/unexpected service-binding rejection; exact project-role checks; backend security tests | Scale-to-zero compute plus request/egress charges. Proxy sidecar adds per-instance CPU/memory. |
| SEC-004, SEC-005 | Passwordless database access, no generated secrets, no authorized networks. | Cloud SQL IAM user, connector enforcement, Auth Proxy sidecar, Python Connector migration job | Static scan and mock plan assertions | No Secret Manager container is created because no application secret is consumed. |
| APP-014 | Structured runtime logs, stable step/run identities, 5xx alert and Terraform-owned notification recipients bound to sensitive versioned create/adopt provenance. | Cloud Run logging plus Monitoring channel, alert policy and budget delivery | Log tests, channel import/provenance digest, enabled/verified/evidence gate tests and post-apply verifier | Logging/Monitoring charges depend on volume; no custom high-volume metric is created. |
| GCP-005, GCP-011 | Bounded dev spend visibility with explicit delivery targets. | Billing budget at 50%, 90%, and 100% thresholds plus required verified email channels | Budget/channel mock assertions; test-email receipt remains a billing-owner gate | Budgets alert but do not cap spend. Default is USD 50 and remains configurable only within a bounded range. |
| GCP-007, GCP-009 | Routine apply cannot substitute source, named container image, plan, workflow or reviewer evidence and cannot administer service IAM or the foundation. It has an exact 16-permission Cloud Run role, repository reader and separate two-permission tag mover without artifact upload/delete; foundation owns project-level service-only `roles/run.servicesInvoker`. The immediate predecessor is existence-checked and tagged only after attestation/preflight. | `environments/dev_runtime`, custom phase IAM, repository IAM, rollback helper, deploy workflow | Exact-attestation ordering; permission probes; exact runtime/tag permission and project-role sets; named app/proxy provenance; rollback baseline/tag proof; WIF/impersonation/state/repository/custom-role drift; public binding denial; smoke; second plan | No added service charge; one moving tag and evidence retention/verification incur small registry/CI/API usage. |
| GCP-003 | Isolated bootstrap/dev projects are always Terraform-managed: new projects are created; existing projects require versioned evidence, acknowledgement and declarative import. Required project labels cannot be overridden. | `google_project` roots and `import` blocks | Parent/billing/project-separation preconditions, adoption negative tests, provenance digests, protected-label tests; no implicit provider project | Project creation/import has no direct service charge; enabled services and resources do. |
| GCP-006 | Runtime project, bootstrap project, region and project/channel provenance must exactly match reviewed foundation outputs; foundation phase identities must be the fixed accounts in that bootstrap project. | `environments/dev` authorization gate and `environments/dev_runtime` foundation gate | Positive binding test plus negative phase-identity/region/project/provenance tests | Prevents accidental cross-project or cross-region latency, authority and egress. |

## Intentionally absent

Pub/Sub, Cloud Tasks, VPC connectors, NAT, load balancers, GKE, additional databases, artifact buckets, and Secret Manager containers are absent because no implemented behavior consumes them. Staging and production contain no resources. These omissions reduce attack surface, drift, and recurring cost.

## Cost confidence

No real target project, open billing account, allowed region, saved plan, or provider price estimate exists. Therefore no exact monthly cost is claimed. Before a real plan, verify shared-core tier availability, storage/backups, Cloud Run sidecar allocation, Artifact Registry retention, logging volume, and regional egress. Infrastructure cost incurred by this task: `0 observed external cloud spend`.
