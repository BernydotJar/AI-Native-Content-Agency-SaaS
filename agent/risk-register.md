# Risk Register

Updated: 2026-07-19T19:08:21Z

| ID | Severity | Risk | Mitigation / gate | Status |
|---|---|---|---|---|
| RISK-001 | Critical | Two active sources of run truth diverge. | Default integrated UI reads only FastAPI state; demo timers require explicit mode; fresh live E2E covers the real transport and restart. | Mitigated locally |
| RISK-002 | High | Approval replay or artifact mutation bypasses reviewer intent or identical concurrent delivery returns a false conflict. | Canonical hash/policy, transaction ownership, direct approval key/audit linkage and exact replay; a fresh evaluator passed 12/12 identical races and the real PostgreSQL gate. | Mitigated locally |
| RISK-003 | High | Development identity is mistaken for production authentication. | Versioned identity boundary, production startup rejection and IAM-private cloud design. | Mitigated for local/dev; production auth deferred |
| RISK-004 | High | Terraform creates public, destructive, broad-IAM or costly resources. | Static vetoes, private IAM, 16-permission routine role without delete/IAM mutation and exact-plan independent gate. | Open; `DENY_APPLY` |
| RISK-005 | Medium | Inline execution is described as resumable mid-step. | Architecture and runbooks claim command-boundary durability only; timeout rollback is tested. | Accepted V1 limitation |
| RISK-006 | High | Cloud SQL or other dev resources incur unreviewed recurring cost. | No plan/apply before target-region pricing, budget recipient proof and independent cost review. | Open external blocker |
| RISK-007 | Medium | Local container success bypasses real browser transport. | Playwright uses the served SPA, FastAPI and PostgreSQL without request interception. | Mitigated locally; current CI pending |
| RISK-008 | Medium | Dependency/provider drift invalidates reproducibility. | Hash locks, frozen uv environment, PyYAML dependency, provider checksums, pinned actions/images and audits. | Mitigated locally; current exact-tree CI pending |
| RISK-009 | Medium | Visual or accessibility regression escapes automated behavior tests. | Manual in-app browser review remains a separate gate. | Open environment limitation |
| RISK-010 | Medium | A lost mutable-command response is mistaken for failure. | Retry only the exact key/payload or refresh; durable replay and duplicate-delivery event counts are tested. | Mitigated locally |
| RISK-011 | High | Cross-tenant identifiers create inconsistent ownership chains. | Composite foreign keys plus application scoping and fresh real PostgreSQL cross-tenant negative flow. | Mitigated locally |
| RISK-012 | High | Routine deploy identity deletes runtime resources or mutates service/foundation IAM. | Split state/identities; repository-scoped image read; exact 16 permissions without delete/setIamPolicy; foundation-managed `roles/run.servicesInvoker`; separate destructive identity/gate. | Mitigated statically; real IAM unproven |
| RISK-013 | High | Mutable GitHub names allow another repository identity to satisfy WIF. | Bind immutable numeric owner/repository IDs plus exact names/ref/direct workflow/environment. | Mitigated statically; critic and real WIF unproven |
| RISK-014 | High | A product-named GCP project is silently adopted or assigned both bootstrap and dev roles. | Explicit CREATE_NEW/ADOPT_EXISTING import evidence and hard distinct-project precondition. | Open external decision |
| RISK-015 | High | A repaired workflow is treated as mandatory before it is on main and exact-tree CI is green. | Main now requires the exact four checks; keep the draft unmerged until current-tree CI and independent review pass. | Partially mitigated; exact-tree CI/workflow-on-main pending |
| RISK-016 | High | Free-text attestation is mistaken for authenticated independent approval. | Protected environments and prevent-self-review are live; a distinct eligible actor/reviewer remains mandatory in addition to attestation. | Open external reviewer gate |
| RISK-017 | High | Static Terraform validation is mistaken for plan/apply readiness. | Every static result returns `DENY_APPLY`; real plan JSON, permission, price, critique and evaluator evidence are separate. | Controlled; apply blocked |
| RISK-018 | Medium | Notification channels point to unreviewed or unverified recipients or are manually created outside state. | Terraform-owned create/import, sensitive versioned provenance, prevent-destroy, exact enabled/verified evidence gate and runtime-bound digest. | Mitigated statically; real recipient proof blocked |
| RISK-019 | High | Governance claims PASS from stale counts, commits or GCP discovery. | Timestamped evidence, strict traceability vocabulary and current readiness report; final evaluator must bind the committed tree. | Mitigating; final evaluation pending |
| RISK-020 | High | Executable eval output omits requirements or turns external blockers into PASS. | Catalog coverage equals traceability exactly; fail-closed schema and blocked gates force `DENY_APPLY`. | In progress under TASK-EVAL-HARNESS-003 |
| RISK-021 | Medium | Concurrent identical run starts execute provider work twice before one transaction wins, creating orphan telemetry or duplicated effects. | Cross-instance tenant/key ownership precedes replay/provider work; a fresh evaluator passed 12/12 races and the real PostgreSQL gate produced exactly one seven-tool workflow. | Mitigated locally |
| RISK-022 | High | A broad project role drifts onto the runtime service account while post-apply verification still passes. | Require the runtime member's exact four-role set and reject every additional binding. | Mitigated locally; live GCP evidence pending |
| RISK-023 | Medium | A digest-pinned runtime image comes from an unreviewed repository. | Bind application and named post-apply image evidence to the foundation repository; pin the proxy to one source-reviewed digest. | Mitigated locally; live GCP evidence pending |
| RISK-024 | Medium | Cleanup removes a previously verified image before rollback. | Preserve the immediate predecessor with one moving KEEP tag after attestation/preflight, using an exact two-permission role, and prove both digests exist. | Mitigated locally; live GCP evidence pending |
| RISK-025 | Medium | Bootstrap/foundation authority drifts after static review without post-apply detection. | Verify live WIF, phase impersonation, state/repository roles and both custom roles against exact evidence. | Mitigated locally; live GCP evidence pending |
| RISK-026 | Critical | A fabricated or stale all-PASS eval file passes CI and masks open vetoes. | Exact catalog/source/aggregate binding and 19 tamper/governance tests; stale results exit 2. | Mitigated locally; result regeneration pending |
| RISK-027 | High | Blank ownership, cyclic dependencies or overlapping active write locks are certified as valid governance. | Typed contracts, dependency DAG, 19-edge critical path, mandatory phase order and active lock exclusivity. | Mitigated locally by 14 checks and negative tests |
| RISK-028 | Medium | A preserved integration database lets concurrency coverage pass only by replaying prior rows. | Fresh owned disposable Compose projects, UUID command namespace and a separate restart scenario. | Mitigated by fresh real PostgreSQL and browser execution |
| RISK-029 | High | A stalled PostgreSQL idempotency-lock holder leaves a duplicate request waiting indefinitely. | Transaction-local 5000ms lock timeout, immediate rollback and redacted 503; six independent focused cases pass. | Mitigated locally; residual bounded contention is Low |
| RISK-030 | High | A local integration gate passes only by adding test-only packages to the deployed runtime. | Build a distinct hash-locked ephemeral test runner; keep the runtime final target and lock unchanged. | Mitigated locally; real gate passed |
| RISK-031 | Medium | A restart test reports failure because Compose restarts a completed migration dependency even though the API and durable row recover. | Restart only the already-created app service, then poll readiness and compare the exact PostgreSQL run. | Mitigated locally; fresh 3/3 browser gate passed |
