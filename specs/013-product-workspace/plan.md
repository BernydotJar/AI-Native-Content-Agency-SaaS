# Plan

1. Record the user-facing critique and product hierarchy.
2. Add a strict server-side provider registry and authenticated GET-only API.
3. Replace the primary App shell with mission-first product navigation.
4. Move theme and provider configuration to Settings.
5. Move tenant credential exchange to a one-time connection dialog.
6. Replace memory internals with applied context and evidence.
7. Replace static Tool Fabric mocks with server/run-derived operational state.
8. Add an integrated loopback-only local product runner.
9. Update claims, documentation and program traceability.
10. Execute focused frontend/backend/browser, package, PostgreSQL, infrastructure,
    security and supply-chain gates.
11. Publish only after a clean-source checkpoint and exact-head CI.

The final broad E2E regression remains deferred to the final program gate, while every
increment continues to run focused interaction, integration and smoke checks.
