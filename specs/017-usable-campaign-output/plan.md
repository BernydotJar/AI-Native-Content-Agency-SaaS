# Plan

1. Change run creation from resource conflict to audited resource replay for an identical
   deterministic run.
2. Preserve strict same-key/different-payload conflicts.
3. Add `CampaignOutputPanel` derived only from durable run artifacts.
4. Compact contextual evidence behind disclosure.
5. Move integration diagnostics into Settings/Admin.
6. Review copy and states with a critic pass.
7. Run focused and cross-workstream gates, commit, push and verify exact-head CI.
8. Keep social publication disabled; specify X OAuth as the next integration increment.
