# INC-028 Tasks

1. Add canonical audit hashing, checkpoint signing and verification types.
2. Extend SQLite schema, backfill and atomic append.
3. Extend PostgreSQL schema v9, backfill and per-tenant serialization.
4. Add integrity checkpoint service/API and startup validation.
5. Add runner, Helm, Terraform and Secret-reference contracts.
6. Update SQLite→PostgreSQL migration and backup/restore verification.
7. Add mutation, deletion, reordering, cross-replica and signing tests.
8. Run producer/critic/fixer/verifier/release gates and persist evidence.
