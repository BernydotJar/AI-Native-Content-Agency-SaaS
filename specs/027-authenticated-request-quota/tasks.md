# INC-027 Tasks

1. Add typed quota exception and atomic SQLite store contract.
2. Add PostgreSQL schema v8, least-privilege grants and cross-replica implementation.
3. Consume quota once in the authenticated principal dependency before CSRF/authorization.
4. Add safe 429 response, `Retry-After` and low-cardinality metrics.
5. Add startup validation and health/readiness configuration disclosure.
6. Add local runner, Helm and Terraform configuration/guards.
7. Add unit, API, migration, PostgreSQL race, backup and package tests.
8. Run producer/critic/fixer/verifier/release gates and persist evidence.
