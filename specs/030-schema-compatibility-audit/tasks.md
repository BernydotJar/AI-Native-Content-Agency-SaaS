# INC-030 Tasks — Runtime Schema Compatibility Audit

1. Add the node to the Graph Harness ledger and graph.
2. Add canonical schema history v1–v9.
3. Implement manifest validation and historical source extraction.
4. Implement SQLite compatibility matrix with data and chain verification.
5. Implement PostgreSQL compatibility matrix inside the existing ephemeral harness.
6. Add negative manifest and unsupported-version tests.
7. Integrate installed-wheel and CI gates with full-history checkout.
8. Perform exact-tree, package, supply-chain, and exact-head review while preserving merge and release gates.
