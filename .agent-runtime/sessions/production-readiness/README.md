# Production Readiness Session

This directory persists the session identity and execution policy. `/goal` is not modeled as a durable project entity. It is a runtime mode that is re-injected when this session resumes.

Current program focus: Terraform + Kubernetes production readiness.
